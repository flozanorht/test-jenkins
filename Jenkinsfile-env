pipeline {
    agent none
    stages {
        sh 'hostname'
        // give it time to check pod creation
        sleep (60)
        oc 'oc whoami'
        oc 'oc whoami -t'
        oc 'oc whoami -c'
        oc 'oc get pod'
    }
}